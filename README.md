# Hybrid RAG System with Automated Evaluation

This project implements a hybrid Retrieval-Augmented Generation (RAG)
system combining dense vector retrieval, sparse keyword retrieval (BM25),
and Reciprocal Rank Fusion (RRF) over a corpus of Wikipedia articles.

## Setup
```
powershell
pip install -r requirements.txt
```